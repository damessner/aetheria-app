/**
 * Keys for hardware-backed secure storage (expo-secure-store).
 * Secrets must NEVER be persisted in AsyncStorage / UserState JSON.
 */
export const SECURE_KEY_STORAGE = 'aetheria.geminiApiKey';
